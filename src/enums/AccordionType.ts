export enum AccordionType {
    Auto = 'auto', // Default accordion flow
    Always = 'always', // Always opened
    Lazy = 'lazy', // Lazy content, only after fist open
    Ever = 'ever', // Generates content after each open
}