import {ValidTextValue} from "../types/ValidTextValue.ts";

export interface IconConfig {
    icon?: ValidTextValue,
    text?: ValidTextValue,
    class?: ValidTextValue,
    pack?: ValidTextValue,
    type?: string,
}